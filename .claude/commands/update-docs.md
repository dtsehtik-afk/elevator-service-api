# Update System Documentation

Update CLAUDE.md to reflect the current state of the codebase.

## Instructions

1. Review all recent changes made in this session (git diff, git log)
2. Update the relevant sections in CLAUDE.md:
   - Add new features to the appropriate sections
   - Update model descriptions if fields were added/changed
   - Update Known Issues / Watch Out For if new pitfalls discovered
   - Update the API Routers table if new endpoints added
   - Update Frontend Pages table if new pages added
3. Commit the updated CLAUDE.md with message: `docs: update CLAUDE.md — <brief description>`
4. Push to `claude/fix-elevator-assignment-error-aUvEz`
5. Remind the user to run `~/deploy.sh`
